# Requirements for Rio Webapp Only (32-bit Raspberry Pi)
# This file is for Raspberry Pi deployment - install to SYSTEM Python (no virtual environment)
# Hardware packages (spidev, RPi.GPIO, picamera) should be installed via apt (system-wide)

# Web framework (upgrades from existing versions)
Flask>=2.0.0,<4.0.0
Flask-SocketIO>=5.4.0,<6.0.0  # Upgraded from 4.3.2 - tested and working
Werkzeug>=2.0.0,<4.0.0
Jinja2>=3.0.0
MarkupSafe>=2.0.0
itsdangerous>=2.0.0
gevent>=23.0.0,<25.0.0
gevent-websocket>=0.10.1

# WebSocket support (compatible with Socket.IO client v2/v3/v4)
# Upgraded versions compatible with Flask-SocketIO 5.4+
python-socketio>=5.14.0
python-engineio>=4.9.0  # Upgraded from 3.13.2 - Version 4.x required for python-socketio 5.x

# Eventlet for async support (required for Flask-SocketIO)
eventlet>=0.33.0,<1.0.0

# Image processing (for droplet detection and camera operations)
# Use headless version for Raspberry Pi (faster installation, pre-built wheels available)
opencv-python-headless>=4.5.0,<5.0.0
numpy>=1.19.0,<2.0.0  # Required for OpenCV and droplet detection (numpy 2.0+ requires 64-bit)
Pillow>=9.0.0  # Required for image processing in camera modules

# Configuration file support
PyYAML>=6.0

# Note: The following hardware packages should already be installed system-wide via apt:
# - spidev==3.6 (install: sudo apt-get install python3-spidev)
# - RPi.GPIO==0.7.1 (install: sudo apt-get install python3-rpi.gpio)
# - picamera==1.13 (install: sudo apt-get install python3-picamera)

# Installation instructions for Raspberry Pi:
# This file installs to system Python (NO virtual environment):
#   python3 -m pip install --user -r requirements-webapp-only-32bit.txt
# Or with sudo for system-wide installation:
#   sudo python3 -m pip install -r requirements-webapp-only-32bit.txt
