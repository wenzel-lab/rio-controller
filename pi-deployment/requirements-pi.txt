# Requirements for Rio Controller - Raspberry Pi Deployment
# This file works for both 32-bit and 64-bit Raspberry Pi OS
# Install to SYSTEM Python (no virtual environment)
# Hardware packages (spidev, RPi.GPIO, picamera) should be installed via apt (system-wide)

# Web framework (compatible with both 32-bit and 64-bit)
Flask>=2.0.0,<4.0.0
Flask-SocketIO>=5.4.0,<6.0.0
Werkzeug>=2.0.0,<4.0.0
Jinja2>=3.0.0
MarkupSafe>=2.0.0
itsdangerous>=2.0.0
gevent>=23.0.0,<25.0.0
gevent-websocket>=0.10.1

# WebSocket support (compatible with Socket.IO client v2/v3/v4)
python-socketio>=5.14.0
python-engineio>=4.9.0  # Version 4.x required for python-socketio 5.x

# Eventlet for async support (required for Flask-SocketIO)
eventlet>=0.33.0,<1.0.0

# Image processing (for droplet detection and camera operations)
# IMPORTANT: Install OpenCV from apt (fast, pre-built): sudo apt-get install -y python3-opencv
# Do NOT install opencv-python-headless from pip - it builds from source (very slow, hours).
# If python3-opencv is not available, uncomment the line below (but expect long build time):
# opencv-python-headless<4.9  # Only if python3-opencv from apt is not available
# Note: numpy<2.0.0 required for 32-bit compatibility (numpy 2.0+ doesn't work on 32-bit)
numpy<2.0.0  # Prefer apt: sudo apt-get install -y python3-numpy
Pillow>=9.0.0  # Required for image processing in camera modules

# Configuration file support
PyYAML>=6.0

# USB serial (syringe pump controller)
pyserial>=3.5

# Note: The following system packages should be installed via apt BEFORE installing Python packages:
# Hardware packages:
#   For 32-bit (legacy Pi camera): sudo apt-get install -y python3-spidev python3-rpi.gpio python3-picamera python3-numpy python3-opencv
# BLAS/LAPACK for droplet detection (provides libcblas.so.3):
#   sudo apt-get install -y libatlas-base-dev libatlas3-base libblas3 liblapack3
# IMPORTANT: Install python3-opencv from apt (fast, pre-built). Do NOT install opencv-python-headless from pip.
#
# Installation instructions for Raspberry Pi:
# Step 1: Install system packages (see above - picamera only)
# Step 2: Install Python packages to system Python (NO virtual environment):
#   python3 -m pip install --user --upgrade pip wheel
#   python3 -m pip install --user -r requirements-pi.txt
# Or with sudo for system-wide installation:
#   sudo python3 -m pip install -r requirements-pi.txt

